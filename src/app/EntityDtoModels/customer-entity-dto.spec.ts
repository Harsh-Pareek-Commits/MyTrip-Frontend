import { CustomerEntityDto } from './customer-entity-dto';

describe('CustomerEntityDto', () => {
  it('should create an instance', () => {
    expect(new CustomerEntityDto()).toBeTruthy();
  });
});
