import { FeedbackEntityDto } from './feedback-entity-dto';

describe('FeedbackEntityDto', () => {
  it('should create an instance', () => {
    expect(new FeedbackEntityDto()).toBeTruthy();
  });
});
